# SAATH static website

These pages describe SAATH's private family deployment model and link to the canonical documentation in the GitHub repository. They intentionally contain no public demo, APK download, release feed, analytics, or live repository API integration.

Serve the files as static assets. The application itself is built from `frontend/`; see the repository README and `docs/SELF_HOSTING.md`.
